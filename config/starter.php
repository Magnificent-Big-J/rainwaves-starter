<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Starter template version
    |--------------------------------------------------------------------------
    |
    | Not a semver of *your* project — this repo has no tagged releases yet
    | (see the v2.0 gap-closure tracker's "Release Candidate" gate). Bump this
    | by hand when a notable milestone actually ships, so `starter:version` has
    | something honest to report rather than a version number nobody set.
    |
    */
    'version' => env('STARTER_TEMPLATE_VERSION', 'unreleased'),

    'repository' => 'https://github.com/Magnificent-Big-J/rainwaves-starter',
];
